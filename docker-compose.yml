version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - API_KEY=${API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - RAPIDAPI_KEY=${RAPIDAPI_KEY}
      - ADZUNA_APP_ID=${ADZUNA_APP_ID}
      - ADZUNA_APP_KEY=${ADZUNA_APP_KEY}
      - DASHBOARD_URL=${DASHBOARD_URL}
    volumes:
      - ./data:/app/data      # persist candidate_profile.json and h1b_sponsors.json
      - ./logs:/app/logs      # persist log files
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    environment:
      - HAWKAPI_URL=http://api:8000
      - HAWKAPI_KEY=${API_KEY}
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

  # Redis — ready for Celery task queue (Phase 2 upgrade)
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
